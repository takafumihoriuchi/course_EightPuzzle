to connect to 理工UNIX:
ssh -l 1w1~50 muse01.mse.waseda.ac.jp
to exit:
exit

改良点：

iterative_deepening_searchで、進行度を表示するために、limitを表示するのはいいが、
	その表示方法を工夫する。
	proceeding to limit : %d\n
	でわかりにくいことはないが、"proceeding to limit : "の繰り返しが助長
	プログレスバーのようなものを導入できると良い。（パーセンテージではなく、何段、的な）

10回の平均、のように、試行回数も指定できるようにする
	CPU-時間に関して、その平均値を自動で計算するようにする

main関数の中で、mapをどの時点で初期化するか。
	whileを繰り返すたびに初期化するか、どのmodeを選んだとしても、最初の条件を同じにするために、whileの外で初期化するか

十回の試行での、探索状態数の平均数、CPU時間の平均値を一連の処理で出力するようにし、
理工UNIXで "./eight_puzzle > output.txt" のようにファイルに書き出すようにしてから、
15パズルに変更する。
